# =======================================================
# Configuração para Superset com Banco de Dados Externo
# =======================================================
# Copie este arquivo para .env e configure suas credenciais

# ---------------------------------------------------------
# 1. Segurança
# ---------------------------------------------------------
# OBRIGATÓRIO: Gere uma chave secreta única para produção
# Exemplo: python -c "import secrets; print(secrets.token_urlsafe(42))"
SUPERSET_SECRET_KEY=MUDE_ESTA_CHAVE_EM_PROD

# ---------------------------------------------------------
# 2. Banco de Dados Externo (Metadados do Superset)
# ---------------------------------------------------------
# Esta é a conexão onde o Superset armazena seus próprios dados
# (dashboards, usuários, permissões, etc.)

# Opção 1: PostgreSQL (Recomendado para produção)
SUPERSET_DATABASE_URI=postgresql://usuario:senha@seu-host-postgres:5432/superset

# Opção 2: MySQL
# SUPERSET_DATABASE_URI=mysql://usuario:senha@seu-host-mysql:3306/superset

# Opção 3: SQLite (não recomendado para produção, apenas dev)
# SUPERSET_DATABASE_URI=sqlite:////app/superset_home/superset.db

# ---------------------------------------------------------
# 3. Credenciais Admin (Primeira Inicialização)
# ---------------------------------------------------------
ADMIN_USERNAME=admin
ADMIN_PASSWORD=admin
ADMIN_EMAIL=admin@superset.com
ADMIN_FIRST_NAME=Admin
ADMIN_LAST_NAME=User

# =======================================================
# INSTRUÇÕES DE USO
# =======================================================
#
# 1. Para usar banco externo PostgreSQL:
#    a) Crie o banco: CREATE DATABASE superset;
#    b) Configure SUPERSET_DATABASE_URI acima
#    c) Execute: docker run --env-file .env -p 8088:8088 superset-custom
#
# 2. Para persistir dados com SQLite:
#    a) Use volume: -v ./superset_data:/app/superset_home
#    b) Execute: docker run --env-file .env -v ./superset_data:/app/superset_home -p 8088:8088 superset-custom
#
